{
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "test": "jest test",
    "t:w": "jest --watch",
    "t:d": "node --inspect-brk ./node_modules/jest/bin/jest --runInBand --watch --no-cache --no-watchman",
    "build": "jest test --coverage",
    "go": "netlify deploy --prod --dir coverage/lcov-report",
    "deploy": "npm run build && npm run go"
  },
  "name": "alds",
  "author": "Nam Nguyen <nam.nguyen.node@gmail.com>",
  "dependencies": {
    "@babel/core": "^7.15.8",
    "@babel/preset-env": "^7.15.8",
    "@babel/preset-typescript": "^7.15.0",
    "@types/jest": "^27.0.2",
    "babel-jest": "^27.2.5",
    "jest": "^27.2.5"
  },
  "prettier": {
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "devDependencies": {
    "netlify-cli": "^6.13.2"
  }
}
