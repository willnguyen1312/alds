import { findMinimumInRotatedArray } from "./findMinimumInRotatedArray"

describe("findMinimumInRotatedArray function", () => {
  it("should work", () => {
    expect(findMinimumInRotatedArray([4, 5, 6, 7, 0, 1, 2])).toEqual(4)
  })
})
